# data-stream-housekeeping

Python module to clean up old files added by data-stream

## Environment variables

All must be set, there are no default values.

* *DATA_DIR*: Directory where data is stored
